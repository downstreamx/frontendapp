import { ProductFormPage } from './ProductFormPage'

/** Edit route — tabbed form aligned with legacy `Items/Edit.tsx` (3 steps, no depot tab). */
export function ProductEditPage() {
  return <ProductFormPage />
}
